# Programmable-Timer-with-Interrupt-Controller-Verilog-RTL-Design-
A Verilog-based programmable timer integrated with a priority interrupt controller. The timer generates periodic interrupts based on a user-defined count, while the controller manages multiple interrupt sources (timer, keyboard, sensor) and forwards prioritized interrupt signals to the CPU
